type Pokemon {
  id: Int!
  name: String!
  image: String
  types: [String!]!
  abilities: [String!]!
  height: Int
  weight: Int
}

type Query {
  randomPokemon: Pokemon!
  pokemon(id: Int!): Pokemon
}
